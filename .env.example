# ============================================================================
# .env.example — Cấu hình Active Defense System
# ============================================================================
# File này đã được cấu hình sẵn, KHÔNG CẦN thay đổi gì.
# Chỉ cần copy thành .env:  cp .env.example .env
# ============================================================================

# === Discord Webhook URL (đã cấu hình sẵn) ===
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/1476897560936452141/LKYWUVlf8DsgbvXromkltHAT5_XPikKHstxE2MilkiOkt-axKPJBG9wD0LW7x5NNpJge

# === Đường dẫn file log (tùy chỉnh nếu cần) ===
AUTH_LOG_PATH=/var/log/auth.log
NGINX_LOG_PATH=/var/log/nginx/access.log

# === Ngưỡng phát hiện SSH Brute-Force ===
# Số lần login thất bại tối đa trong khoảng thời gian (giây)
SSH_BRUTE_FORCE_THRESHOLD=5
SSH_BRUTE_FORCE_WINDOW=60

# === Ngưỡng phát hiện HTTP Flood ===
# Số request tối đa từ 1 IP trong khoảng thời gian (giây)
HTTP_FLOOD_THRESHOLD=100
HTTP_FLOOD_WINDOW=10

# === IP Whitelist (cách nhau bởi dấu phẩy) ===
# Các IP trong danh sách này sẽ KHÔNG BAO GIỜ bị block
WHITELISTED_IPS=127.0.0.1,172.20.0.10

